# M.A.P.S 고교학점제 과목선택 네비게이터 — UI/UX 검토 보고서 (v1)

- **검토일**: 2026-07-12
- **검토 대상**: `views/course_navigator.py` (S0~S7 전 화면)
- **검토 기준**: 화면설계 v1.1(`MAPS_자체서비스_화면설계_v1.md`), FABRIK 디자인 시스템(`views/highschool.py` — 무채색 에디토리얼·잉크 반전·8px/999px 라운드), Streamlit 렌더 제약, 모바일 우선
- **검토자 관점**: 모바일 웹 UI/UX + 교육 서비스 + Streamlit 실무

---

## 1. 요약 (Executive Summary)

전체 흐름(S0→S7)은 화면설계 v1.1의 깔때기 구조를 충실히 구현했고, FABRIK 표 스타일(`cn-univtb`)·상태 초기화·뒤로가기 이력 스택·공유 링크는 견고합니다. 다만 **정보를 계속 얹으면서 화면당 밀도가 설계 원칙("한 화면 = 한 질문 = 한 결정")을 넘어섰고**, 특히 **S3가 과부하 상태**입니다. 색 사용은 유형 구분(핑크/초록/파랑 pill) 도입으로 FABRIK 무채색 원칙에서 이탈했는데, 이건 의도적 트레이드오프이므로 "일관되게 쓰느냐"가 관건입니다.

### P0 (사용성 훼손 — 출시 전 수정 권장)

| # | 화면 | 문제 | 핵심 수정 |
|---|---|---|---|
| P0-1 | S3 | **과목명 자체가 토글 버튼**이라 "선택"인지 "펼치기"인지 모호(사용자 제기) | 과목명은 텍스트로, 오른쪽에 **[내용 보기] 아이콘 버튼** 분리 |
| P0-2 | S3 | 한 과목 펼치면 성취기준+연계학과 표+이수버튼이 쏟아짐, expander 3개 연속 | 펼침 내용 **2단 축약**, 하단 아코디언 통합 |
| P0-3 | S4 | **개별 "담기" 버튼 + 체크박스 "체크한 항목 담기"** 두 담기 경로 공존 → 혼란 | 체크박스 방식 하나로 통일(개별 담기 제거) |
| P0-4 | S3·S4 | **"미개설"과 "확인 필요"가 같은 회색 뱃지** → 구분 불가 | 미개설은 별도 톤(외곽선 점선 등), 문구 차별화 |
| P0-5 | 전역 | **이수 상태 표기 3종 불일치**: S3 `✓/📝` · S4 `하는 중/신청함` 텍스트 · S5/S7 `✓/📝` | 한 가지 표기 규칙으로 통일 |

### P1/P2는 §5 백로그 참조.

---

## 2. 사용자 제기 이슈 — S3 과목 토글 (심층)

### 2.1 현재 구조의 문제

```python
# 현재: 과목명 전체가 토글 버튼
if cols[1].button(f"{arrow} {s}{mark}", key=f"cn_s3btn_{typ}_{s}"):
    st.session_state[open_key] = not is_open
```

- **어포던스 충돌**: 사용자는 목록에서 "과목명 = 클릭하면 그 과목을 고르는 것"으로 학습돼 있음(S2의 학과 카드가 그렇게 동작). 그런데 여기선 "펼치기"라 기대와 어긋남.
- **화살표(▸/▾)의 약한 신호**: 토글임을 알리는 유일한 단서가 앞의 작은 화살표뿐. 과목명이 길면 화살표가 시선 밖으로 밀림.
- **행 전체가 버튼이라 시각적으로 무거움**: 30개 과목이 전부 큰 버튼 블록 → 목록 스캔이 어려움(첨부 스크린샷의 "회색 버튼 벽" 현상).

### 2.2 대안 비교 (Streamlit 제약 안)

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **A. 과목명 텍스트 + 오른쪽 [＋보기] 아이콘 버튼** | 어포던스 명확(텍스트≠클릭타깃, 버튼만 클릭). 행이 가벼워짐 | 열 하나 더 필요(현 3열→4열, 모바일 폭 압박) | **권고** |
| B. `st.expander`로 과목 하나=아코디언 | 표준 위젯, 펼침 아이콘 명확 | expander 라벨에 HTML(유형 pill) 불가 → 유형 구분 약화. 이미 하단에 expander 3개라 남발 가중 | 반려 |
| C. `st.popover`(팝오버)로 상세 | 목록 안 깨짐, 오버레이로 상세 | 모바일에서 팝오버 폭 좁음, 성취기준+표 담기엔 작음 | 조건부 |
| D. 상세를 모달(`st.dialog`)로 | 목록 완전히 가볍게 유지, 상세는 풀스크린 | 클릭→모달 rerun 비용, 이미 모달(첫방문·대안) 있어 모달 남발 | 조건부 |

### 2.3 권고안 (대안 A + 정보 다이어트)

**행 레이아웃 변경** — 과목명은 순수 텍스트, 상세는 우측 아이콘 버튼:

```python
# ROW3 = [유형pill 0.9 · 과목명(텍스트) 2.6 · 개설여부 1.3 · 보기버튼 0.7]
cols = st.columns([0.9, 2.6, 1.3, 0.7], vertical_alignment="center")
cols[0].markdown(_pill_html(typ), unsafe_allow_html=True)
cols[1].markdown(f"{s}{mark_html}")           # 과목명은 클릭 대상 아님
cols[2].markdown(offered_badge, ...)          # 개설여부
if cols[3].button("＋", key=..., help="배우는 내용 보기"):   # 상세는 이 버튼만
    st.session_state[open_key] = not is_open
    st.rerun()
```

- 모바일 640px 이하에서 4열이 좁으면, 개설여부를 아이콘(✓/―/?)으로 축약하거나 유형 pill을 과목명 앞 인라인으로 합쳐 3열 유지.
- **이수 버튼은 펼침 영역에서 빼고**, S4의 이수 입력과 통합하거나(§4-S4 참조) 과목명 옆 작은 토글로 이동 — 지금은 "펼쳐야만 이수 체크 가능"이라 발견성이 낮음.

---

## 3. FABRIK 원칙 대비 색 사용 판정

FABRIK 원칙(highschool.py L108): **"강조는 색이 아니라 잉크 반전."** 현재 course_navigator는 이를 3곳에서 이탈:

| 요소 | 현재 색 | 판정 |
|---|---|---|
| 유형 pill (일반=핑크/진로=초록/융합=파랑) | 3색 | **의도적 트레이드오프 — 허용하되 채도 낮춰 통일**. 유형은 반복 등장 라벨이라 색 구분이 스캔에 유리. 단 현재 채도(#B23A48 등)가 무채색 베이스에서 튐 → 파스텔로 한 단계 낮추기 권고 |
| 근거 문장 `reason` (#1D4ED8 파랑) | 파랑 텍스트 | **위반 — 무채색으로**. 근거는 강조가 아니라 보조 정보. `muted` 회색이 맞음 |
| 개설 뱃지 `ok` (#1D4ED8 파랑) | 파랑 | **부분 위반**. "개설"은 긍정 신호라 강조가 필요하나, 파랑 대신 잉크 채움(검정 배경 흰 글씨)이 FABRIK 정석 |
| `st.success` 초록 박스(S0 학교 선택) | 초록 | **위반 — Streamlit 기본 초록**. `cn-banner`나 잉크 톤 캡션으로 교체 |
| 성취표 하이라이트 행(#FFF8EC 크림) | 크림 | 허용(연한 배경, 튀지 않음) |

**핵심 판단**: pill 3색은 유지하되 **채도를 낮춰 "무채색 베이스 + 은은한 유형색"**으로 정돈하고, 그 외 파랑/초록(근거·개설·success)은 무채색/잉크로 환원. 지금은 "무채색인데 파랑·초록·핑크가 산발"이라 시스템이 없어 보임.

---

## 4. 화면별 상세

### S0 — 학교·학년
**잘된 점**: 시작 버튼 `disabled`로 미완성 진입 차단, 데이터 출처 캡션.

| 문제 | P | 수정 |
|---|---|---|
| 학교 선택 시 `st.success`(초록) — FABRIK 위반 | P1 | `st.markdown('<div class="cn-banner">✓ …</div>')` 잉크 톤으로 |
| `st.columns(3)` 시도·시군구·학교 — 모바일 640px에서 셀렉트 3개가 좁아 학교명 잘림 | P1 | 640px 이하 세로 1열(`cn_chip_grid`식 미디어쿼리) 또는 라벨 위 배치 |
| 학기 radio가 `label_visibility="collapsed"` — "1학기/2학기"가 뭔지 문맥 없음 | P2 | 위에 `**학기**` 라벨 추가(학년처럼) |
| "고1·고2만" 안내가 배너로 상시 노출 — 첫 화면부터 제약 강조 | P2 | 캡션 톤으로 낮추거나 학년 선택 근처로 이동 |

### S1 — 관심 입력
**잘된 점**: 발화+칩 이중 입력, 빈 입력 방어, 칩 24개 큐레이션.

| 문제 | P | 수정 |
|---|---|---|
| **칩 선택 개수 미표시** — "2~3개"라면서 지금 몇 개 골랐는지 안 보임. 3개 초과 시 조용히 무시(피드백 없음) | P1 | 칩 그리드 위 `선택 2/3` 카운터, 3개째 선택 시 안내 |
| 선택 칩이 primary(검정 채움) — 24개 중 2~3개만 검정이라 대비는 좋으나, `말한 걸로 찾기`도 primary라 **primary 2종 공존** | P2 | 칩 선택은 "잉크 외곽선"(secondary+테두리 진하게)으로, primary는 CTA 1개만 |
| 발화 예시 placeholder 고정 1개 | P2 | 3~4개 로테이션(설계 v1.1 의도) |

### S2 — 갈래·후보 카드
**잘된 점**: 대분류 그룹핑, 근거 문장, 점수·순위 없음(설계 원칙 준수), 선발대학 표.

| 문제 | P | 수정 |
|---|---|---|
| **학과 "선택" 버튼이 secondary** — 이 화면의 핵심 전진 액션인데 위계가 약함. `선발대학 보기`(expander)와 시각 무게 비슷 | P1 | 학과 선택을 primary 또는 카드 전체를 클릭 타깃으로. 최소한 화살표(→) 부여 |
| 근거 문장 파랑(#1D4ED8) | P1 | §3 참조 — muted로 |
| `cn-major .summary` 요약 길이 무제한 — 커리어넷 개요가 3~4문장이면 카드가 김 | P2 | 2줄 클램프(`-webkit-line-clamp`) |
| 카드 안에 요약+근거+선발대학 expander+선택버튼 → 카드 1개가 큼(한 갈래 3학과면 스크롤 상당) | P2 | 선발대학 expander를 S3로 미루고 카드는 이름+근거+선택으로 경량화 |

### S3 — 학과 상세 (**최다 지적**)
**잘된 점**: 개설여부를 과목별로 미리 표시(설계 의도 정확), "대학이 실제로 보는 과목" 차별 정보.

| 문제 | P | 수정 |
|---|---|---|
| 과목명=토글 버튼 어포던스 | P0-1 | §2 권고안 |
| 펼침 1개에 성취기준(3개+더보기)+연계학과 표(5행)+이수 버튼 2개 — 정보 폭발 | P0-2 | 성취기준 2줄 미리보기+더보기, 연계학과는 "N개 학과와 연결" 캡션+접힘, 이수 입력 분리 |
| **하단 expander 3연속**(선발대학 보기[S2 중복]·대학별 자세히·설치대학 더보기) | P0-2 | 설치대학은 1곳으로 통합(S2 선발대학과 중복 제거), 대학별 상세는 유지 |
| "대학이 실제로 보는 과목" 불릿 8개+expander+배너 — 한 섹션이 너무 김 | P1 | 상위 3~4개만 노출+더보기, 배너는 캡션으로 |
| 개설여부 "미개설"·"확인 필요" 동일 회색 | P0-4 | 미개설=점선 외곽 회색, 확인필요=`?` 아이콘 톤 분리 |
| 맨 아래 "확인하러 가기" primary와, 펼침 안 "성취기준 전체보기"·이수 버튼(선택 시 primary)이 시각 경쟁 | P1 | 페이지 CTA는 primary 1개 원칙 |

### S4 — 우리 학교 현황
**잘된 점**: 성취표 상위5+더보기(★ 강조), 개설 요약 카운트, 전체선택+체크 담기, 첫 방문 안내 모달.

| 문제 | P | 수정 |
|---|---|---|
| **개별 "담기" 버튼 + 체크박스 "체크한 항목 담기" 이중 경로** | P0-3 | 하나로: 체크박스 방식만 남기고 개별 담기 제거(또는 반대). 지금은 같은 목표에 2 UI |
| 상태칸 `cn-statusbtn`(회색 비활성 "하는 중"/"신청함"/"담음")이 실제 "담기" 버튼과 **모양이 유사**(둘 다 라운드 박스) → 클릭 가능/불가 혼동 | P0-3 | 비활성 상태는 뱃지(작은 pill)로, 액션만 버튼으로 명확히 분리 |
| 체크박스 `transform: scale(1.6)` — 터치는 커졌으나 정렬(우측)과 행 높이 흔들림 | P2 | 셀 폭·정렬 재조정, 라벨 클릭 영역 확대 |
| 성취표 8열(과목·학년학기·평균·A~E) — 모바일 가로 스크롤 유발 | P1 | 640px 이하에서 A~E를 접거나 평균만+상세토글, 표에 `overflow-x:auto` 래퍼 |
| "이미 하는 중/담을 수 있어/없어/확인 필요" 요약과 표의 상태 라벨(하는 중/신청함/담음/확인 필요)이 **부분만 일치** | P1 | 용어 사전 통일(§5) |

### S5 — 상의할 목록
**잘된 점**: plan/taken 분리, 빈 상태 처리, 진로 역참조.

| 문제 | P | 수정 |
|---|---|---|
| "공유 링크 만들기" → **"주소창의 링크를 복사"** — 모바일에서 주소창 복사는 매우 불편 | P1 | 링크를 `st.code`(복사 아이콘 내장)로 출력하거나 `st.text_input`+전체선택 안내 |
| "빼기" 버튼이 각 행 우측 — 실수 삭제 방지 없음 | P2 | 확인 없이 즉시 삭제는 OK나, 삭제 후 `st.toast` 피드백 |
| taken은 "이미 듣고 있거나 신청한 과목"인데 S4 요약은 "이미 하는 중" — 용어 흔들림 | P2 | §5 |

### S6 — 미개설 대안(모달)
**잘된 점**: 화면 이동 없이 모달, 공동교육과정/교실온닷 안내(설계 정확).

| 문제 | P | 수정 |
|---|---|---|
| 모달 안 "닫기" primary 버튼(풀폭) — 안내 모달인데 CTA가 무거움 | P2 | secondary·짧게, 또는 X만 |
| 대안 내용이 정적(모든 미개설 과목에 동일 문구) | P2 | 최소한 과목명만 상단에 크게(현재 있음) — OK |

### S7 — 공유 읽기전용 뷰
**잘된 점**: 서버 저장 없이 URL 복원, "나도 해보기" 전환.

| 문제 | P | 수정 |
|---|---|---|
| 라벨 **"이수 체크"**(S7) vs "이미 듣고 있거나 신청한 과목"(S5) — 같은 데이터 다른 이름 | P1 | 통일 |
| 헤더 "학생 플랜 요약" — 교사가 볼 화면인데 톤이 학생 지칭 | P2 | "○○님의 과목 선택 초안" 등 |
| 디코드 실패 시 `st.error`만 — 되돌아갈 CTA 없음 | P2 | "처음부터 해보기" 버튼 |

---

## 5. 일관성 감사

| 개념 | 현재 표기(화면별) | 문제 | 통일안 |
|---|---|---|---|
| 이수 상태 | S3 `✓`/`📝` · S4 `하는 중`/`신청함` 텍스트 · S5·S7 `✓`/`📝` | 3종 혼재 | **아이콘+짧은 라벨 1종**: `✓ 이수`, `📝 신청` 전 화면 동일 |
| 개설 상태 | `우리 학교 개설`(파랑) / `미개설`(회색) / `확인 필요`(회색) / S4 요약 `담을 수 있어`·`없어` | 뱃지와 요약 용어 다름, 미개설·확인필요 동색 | 뱃지=요약 용어 일치, 3색 톤 분리 |
| 유형 라벨 | pill 3색 일관 사용(일반/진로/융합) | 색이 FABRIK에서 튐 | 채도 하향, 유지 |
| 표 스타일 | `cn-univtb` 하나로 통일 | **일관 — 잘됨** | 유지 |
| 액션 버튼 위계 | primary 남용(S1 칩·CTA, S3 이수·확인, S4 담기·상태) | 한 화면 primary 다수 | **화면당 primary 1개**(전진 CTA만) |
| 정보 접힘 | expander(S2 선발대학·S3 3개·성취기준) + 커스텀 토글(S3 과목·S4 더보기) | 접힘 UI 2계열 혼용 | 접힘은 expander로, 커스텀 토글은 "더보기"류만 |

---

## 6. 우선순위 백로그

### P0 — 사용성 훼손 (출시 전)
1. **S3 과목 토글 → 과목명 텍스트 + 우측 [보기] 버튼**(§2 권고안). ROW3 4열 재배치, 이수 버튼 분리.
2. **S3 펼침·하단 정보 다이어트**: 성취기준 2줄 미리보기, 연계학과 접힘, expander 3→통합, "대학이 보는 과목" 상위3+더보기.
3. **S4 담기 경로 단일화**: 체크박스 방식만(개별 "담기" 제거) + 비활성 상태를 pill로 바꿔 버튼과 구분.
4. **개설 상태 3색 톤 분리**: 개설(잉크 채움)·미개설(점선 회색)·확인필요(`?` 톤). 미개설≠확인필요.
5. **이수 상태 표기 1종 통일**(✓ 이수 / 📝 신청) 전 화면.

### P1 — 개선 권장
6. S2 학과 선택 버튼 primary/카드 클릭화(위계 강화).
7. 파랑·초록·success → 무채색/잉크 환원(§3): 근거 문장, 개설 뱃지, S0 success.
8. S1 칩 선택 카운터(2/3) + 3개 제한 피드백.
9. S5 공유 링크를 `st.code`(복사 가능)로.
10. S4 성취표 모바일 가로 스크롤 처리(`overflow-x:auto`).
11. S0 셀렉트 3개 모바일 세로 배치.
12. S7 "이수 체크" 용어 통일, 교사 톤 헤더.

### P2 — 다듬기
13. pill 채도 하향(파스텔).
14. S2 요약 2줄 클램프.
15. S1 placeholder 로테이션.
16. S0 학기 라벨 노출.
17. S6 모달 닫기 버튼 경량화.
18. 삭제·담기 후 `st.toast` 피드백 통일.

---

## 7. 총평

- **구조는 옳다**: 깔때기 흐름·데이터 연결·상태 관리는 설계 의도대로.
- **문제는 "밀도"다**: 기능을 계속 얹으면서(대교협 권장과목·성취표·연계학과·설치대학) 화면당 정보가 "한 화면=한 결정" 원칙을 넘어섰다. 특히 S3는 5개 정보 블록이 한 스크롤에 쌓여 있다. **더하기보다 접기·미루기·통합**이 이 단계의 과제.
- **색 시스템을 결정하라**: "무채색 + 유형색만" 원칙을 명문화하고, 그 외 파랑·초록을 정리하면 지금의 "산발적" 인상이 "의도된 시스템"으로 바뀐다.
- 우선 P0-1(사용자 제기)·P0-3(담기 이중경로)·P0-5(이수 표기)부터 처리하면 체감 개선이 가장 크다.
