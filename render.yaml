services:
  - type: web
    name: maps-course-navigator
    runtime: python
    plan: free
    buildCommand: pip install -r webapp/requirements.txt
    startCommand: uvicorn webapp.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
